(ns closh.core
  (:require [clojure.string]))
            ; [closh.parser]))

; (defmacro sh [& tokens]
;   (closh.parser/process-command-list (s/conform :closh.parser/cmd-list tokens)))

; (macroexpand '(sh ls -l))
